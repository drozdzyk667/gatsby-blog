import * as React from 'react'
import Helmet from 'react-helmet'
import Layout from '../../components/layout'

const Austria = () => {
  return (
    <Layout>
      <Helmet>
        <title>Podróże Przez Austrię</title>
        <meta name="description" content="Right Sidebar Page" />
      </Helmet>

      <div>Austria</div>
    </Layout>
  )
}

export default Austria
